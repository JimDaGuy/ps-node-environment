/*
Used for webpack to bundle vendor libraries separately
in production. This prevents re-downloading application
code when only a vendor library changes
*/

/* eslint-disable no-unused-vars */
import fetch from 'whatwg-fetch';
//Would normally hold JQuery, Angular, React, etc.
